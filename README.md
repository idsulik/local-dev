### Docker compose watch && Skaffold && Tilt usage example

#### Docker compose watch
```bash
docker compose watch
```

#### Skaffold
```bash
skaffold dev --port-forward=pods
```

#### Tilt
```bash
tilt up
```